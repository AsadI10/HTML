I haven't attempted the database/dynamic part. 
As a bonus feature which can be found on contact us page as hyperlink to sign up, is that the user can click on show password and it will show the password,
it's a toggle method as soon clicked the password will appear, unclicked it will disappear.