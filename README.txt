I haven't attempted the database/dynamic part. 
As a bonus feature which I created in my HTML is that the user can click on show password and it will show the password,
it's a toggle function as soon clicked the password will show unclicked it will disappear.